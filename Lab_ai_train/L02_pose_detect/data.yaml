# 데이터셋 경로 설정 (절대 경로 또는 상대 경로)
path: ../datasets/
train: images/train
val: images/val

# 클래스 설정 (포즈 추정은 보통 '사람' 1개 클래스만 사용)
names:
  0: person

# 💡 포즈(키포인트) 필수 설정
# [키포인트 개수, 차원 수]를 의미합니다.
# 차원 수가 3인 경우: [x좌표, y좌표, 가시성(visible)]
# 차원 수가 2인 경우: [x좌표, y좌표]
kpt_shape: [6, 3]